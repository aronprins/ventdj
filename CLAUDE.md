# CLAUDE.md

This project's guidance for Claude Code and other agents lives in
**[AGENTS.md](AGENTS.md)** — read it first.

It covers the project layout, the automated cache-busting version stamp
(`build.sh` + `.githooks/pre-commit`), how to rebuild the post index, how to
preview locally, and the code conventions to follow.
